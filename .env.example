# Point both client endpoints at the landing-pages-system platform (port 3001).
# This landing app contains NO secrets — it only sends events and leads to the platform.
NEXT_PUBLIC_ANALYTICS_API_URL=http://localhost:3001/api/analytics/event
NEXT_PUBLIC_INBOX_URL=http://localhost:3001/api/inbox
